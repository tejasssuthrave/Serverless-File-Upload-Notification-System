# Serverless-File-Upload-Notification-System
This project demonstrates a serverless, event-driven architecture built on AWS. Whenever a file is uploaded to an S3 bucket, an AWS Lambda function is triggered, which processes the file metadata and sends a real-time email notification using Amazon SNS.
